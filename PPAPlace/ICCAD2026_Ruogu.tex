%%
%% Commands for TeXCount
%TC:macro \cite [option:text,text]
%TC:macro \citep [option:text,text]
%TC:macro \citet [option:text,text]
%TC:envir table 0 1
%TC:envir table* 0 1
%TC:envir tabular [ignore] word
%TC:envir displaymath 0 word
%TC:envir math 0 word
%TC:envir comment 0 0

%%
%% For anonymous submission (double-blind review), use:
\documentclass[sigconf,anonymous,review,9pt]{acmart}

%%
\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

%%
\setcopyright{acmlicensed}
\copyrightyear{2026}
\acmYear{2026}
\acmDOI{XXXXXXX.XXXXXXX}
\acmConference[ICCAD'26]{IEEE/ACM International Conference on Computer-Aided Design}{November 8--12, 2026}{San Jose, CA, USA}
\acmISBN{978-1-4503-XXXX-X/26/11}

%%
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{booktabs}
\usepackage{subcaption}

\usepackage{caption}
\captionsetup{font=small}

\begin{document}

\title{PPAPlace: Enhancing Chip Placement via Mixed-Size PPA Prediction}

\author{Ruogu Chen}
\affiliation{%
  \institution{University of Alberta}
  \department{Department of Electrical and Computer Engineering}
  \city{Edmonton}
  \state{AB}
  \country{Canada}
}
\email{ruogu@ualberta.ca}

\author{Jie Han}
\affiliation{%
  \institution{University of Alberta}
  \department{Department of Electrical and Computer Engineering}
  \city{Edmonton}
  \state{AB}
  \country{Canada}
}
\email{jhan8@ualberta.ca}

%% ============================================================
\begin{abstract}
Macro placement significantly determines the chip's post-route performance, power, and area (PPA). Most chip placers optimize the half-perimeter wirelength (HPWL), yet recent benchmarking shows a near-zero correlation between HPWL and post-route timing metrics, and all evaluated AI placers degrade PPA relative to hierarchical baselines. Recent efforts such as LaMPlace train cross-stage predictors to close this gap, but rely on macro-centric representations with pre-routing supervision. This paper presents PPAPlace, a placer-agnostic framework that directly predicts post-route PPA from the full mixed-size placement state. A systematic label fidelity study across design flow stages shows that post-global-routing metrics best reflect final PPA while remaining cheap to generate; in contrast, pre-route metrics can reverse placement rankings. A dual-stream predictor combining graph attention over the netlist with spatial convolution over the placement grid is trained on these post-GRT labels. Because the predictor takes any finished placement as input, it serves as a fast surrogate objective for configuration optimization across different placement paradigms. PPAPlace deploys this surrogate through Bayesian optimization over the placer's hyperparameter space, finding configurations that lead to high-quality placements. PPAPlace is evaluated on five unseen ChiPBench circuits and consistently outperforms baseline placement methods, achieving average WNS and TNS improvements of 12\% and 21\%, respectively.
\end{abstract}

\keywords{chip placement, PPA prediction, Bayesian optimization, surrogate-guided optimization, graph attention network, physical design}

\maketitle

%% ============================================================
\section{Introduction}
\label{sec:intro}

Chip placement determines the physical locations of circuit modules on a
two-dimensional canvas and is one of the most significant steps in
VLSI physical design. The quality of placement directly affects the
chip's final performance, power consumption, and area (PPA), which
together determine whether a design meets its specifications and can be
manufactured~\cite{kahng2022vlsi}.

Existing placement approaches fall into two broad categories: analytical and AI-based placers. Analytical
placers such as DREAMPlace~\cite{lin2019dreamplace} formulate placement
as continuous optimization of a smooth wirelength approximation plus
density penalties, solved by gradient descent on GPU. AI-based methods
including reinforcement
learning~\cite{mirhoseini2021graph, lai2022maskplace, lai2023chipformer},
black-box optimization~\cite{shi2023wire}, and diffusion
models~\cite{lee2024chip} replace or augment the optimizer with learned
policies. Despite the methodological differences between these two categories, most methods focus only on optimizing the half-perimeter wirelength (HPWL), a fast estimate of total wire length, as the primary placement objective.

However, growing evidence suggests that HPWL is a poor proxy for the metrics that
matter in practice. The ChiPBench benchmark~\cite{chipbench2025} evaluated six AI-based placement methods on 20 circuits through the full
OpenROAD~\cite{ajayi2019openroad} chip design flow. ChiPBench uses
Hier-RTLMP, OpenROAD's built-in hierarchical macro placer, as the
baseline placement method. Hier-RTLMP leverages RTL design hierarchy and dataflow
structure rather than optimizing wirelength alone, yet every evaluated AI
method degraded PPA relative to it. ChiPBench further reported a Pearson
correlation of only $-0.08$ between macro HPWL and the worst negative slack
(WNS), confirming that wirelength optimization is essentially
uninformative for timing.

Recent efforts have begun to address this disconnect. LaMPlace~\cite{lamplace2025} trains a cross-stage predictor on
offline placement data and uses it to guide macro placement through
evolutionary search, achieving notable improvements in timing metrics.
While these results confirm that cross-stage prediction is a viable
path to better placement, existing approaches share two unexamined
assumptions. First, they adopt pre-route timing as the supervision
data without verifying that such labels faithfully preserve PPA information
as observed after routing. Second, they represent the placement
through macro positions alone, discarding the standard cell density
and routing congestion that ultimately govern timing and power.

This paper carefully evaluated both assumptions. A controlled study across 10 ChiPBench circuits reveals that pre-route timing labels, which were used as supervision data in prior work, can be misleading. Post-global-routing (GRT) labels, by contrast, consistently achieve high rank correlations with final sign-off PPA and remain inexpensive to generate. This finding establishes a principled basis for supervision stage selection in any cross-stage learning approach, whether for PPA prediction, placement optimization, or design-space exploration.

Building on this finding, PPAPlace trains a dual-stream predictor on post-GRT labels over the complete mixed-size placement state, encoding
netlist connectivity via graph attention and spatial density via
convolutional analysis. Because the predictor evaluates the placement itself rather than any placer-specific state, the same trained surrogate readily applies to any existing or future placer with tunable configurations. PPAPlace deploys this surrogate
through Bayesian optimization (BO) over the placer's configuration
space to find configurations that lead to high-quality placements.

The contributions are as follows:

\begin{enumerate}
    \item A label fidelity study across 10 circuits and four design stages, establishing post-global-routing as the most cost-effective supervision stage for cross-stage PPA prediction.

    \item A dual-stream predictor over the complete mixed-size placement state, achieving nearly $4\times$ higher ranking accuracy than the macro-only, pre-route paradigm.

    \item A surrogate-guided optimization framework that improves WNS by 12\% and TNS by 21\% over the hierarchical baseline in 50 trials. Because the predictor operates on finished placements rather than placer internals, the framework is architecturally placer-agnostic.
\end{enumerate}

The remainder of this paper is organized as follows. Section~\ref{sec:related} reviews analytical, AI-based, and cross-stage prediction approaches to chip placement. Section~\ref{sec:prelim} formulates the cross-stage PPA prediction problem. Section~\ref{sec:label_fidelity} presents the label fidelity study that identifies post-global-routing as the most effective supervision stage. Section~\ref{sec:method} describes the PPAPlace framework, including the mixed-size placement representation, the dual-stream predictor architecture, and the surrogate-guided configuration optimization mechanism. Section~\ref{sec:experiments} evaluates PPAPlace on ChiPBench circuits through the full RTL-to-signoff flow. Section~\ref{sec:conclusion} concludes the paper.

%% ============================================================
\section{Related Work}
\label{sec:related}

\paragraph{Analytical placement}
Analytical placers formulate placement as continuous optimization of a
smooth wirelength objective subject to density
constraints~\cite{lu2015eplace, cheng2019replace}.
DREAMPlace~\cite{lin2019dreamplace} recast this formulation as a neural
network training problem, achieving over $30\times$ GPU speedup.
DREAMPlace~4.0~\cite{dreamplace4} added net weighting driven by static
timing analysis to prioritize timing-critical nets. However, these net
weights are derived from pre-route STA estimates whose fidelity we
question in Section~\ref{sec:label_fidelity}.
AutoDMP~\cite{autodmp2023} introduced Bayesian optimization to tune
DREAMPlace hyperparameters automatically. It uses HPWL as the BO
objective, inheriting the same HPWL-to-PPA gap. PPAPlace replaces HPWL
with a learned PPA surrogate as the BO objective. Commercial tools such
as Synopsys DSO.ai~\cite{dsoai2025} and Cadence
Cerebrus~\cite{cerebrus2025} apply reinforcement learning to tune tool
configurations at a larger scale.

\paragraph{AI-based placement}
AlphaChip~\cite{mirhoseini2021graph} pioneered deep reinforcement
learning for macro placement, though its reproducibility remains
debated~\cite{kahng2023reevaluating}. Subsequent work explored visual
representation learning~\cite{lai2022maskplace}, offline
RL~\cite{lai2023chipformer}, evolutionary
search~\cite{shi2023wire}, placement
refinement~\cite{maskregulate2024}, and tree-search-guided
RL~\cite{efficientplace2024}. Despite this diversity, all methods
optimize HPWL or macro HPWL as the primary objective.

\paragraph{Cross-stage PPA prediction and optimization}
LaMPlace~\cite{lamplace2025} trains a Laurent polynomial predictor on
offline data to estimate cross-stage metrics from macro pairwise
distances. It uses the predictor to guide WireMask-EA's evolutionary
search, achieving notable timing improvements. Its training labels come
from DREAMPlace cell placement followed by OpenTimer static timing
analysis, providing pre-route supervision.
MacroRank~\cite{macrorank2023} and
PreRoutGNN~\cite{preroutgnn2024} predicted placement or routing quality
but did not use these predictions to improve placement.
ReMaP~\cite{re2map2025} and
BBOPlace-Bench~\cite{bboplacebench2025} highlight the importance of
aligning the search objective with downstream PPA rather than HPWL.
Across these efforts, a common gap remains: no work has systematically
validated which design flow stage provides the most reliable supervision
for cross-stage prediction.


%% ============================================================
\section{Preliminaries}
\label{sec:prelim}

\subsection{Chip Placement and Evaluation Metrics}

Chip placement assigns physical positions to circuit modules on a
two-dimensional canvas. A netlist hypergraph $H = (V, E)$ specifies the
modules $V$ (macros and standard cells) and the nets $E$ connecting
them. Placement methods seek positions
$\mathbf{x} = \{(x_i, y_i)\}_{i=1}^{N}$ that minimize a placement
objective. In analytical placers such as
DREAMPlace~\cite{lin2019dreamplace}, this takes the form:
\begin{equation}
\label{eq:placement}
\min_{\mathbf{x}} \; \mathcal{L}(\mathbf{x})
= \mathcal{W}(\mathbf{x}) + \lambda \cdot \mathcal{D}(\mathbf{x})
\end{equation}
where $\mathcal{W}$ is a smooth wirelength approximation and
$\mathcal{D}$ is a density penalty that discourages cell
overlap~\cite{lu2015eplace}. The density weight $\lambda$ increases
iteratively to enforce legality. The wirelength term is typically based
on HPWL:
\begin{equation}
\label{eq:hpwl}
\mathrm{HPWL}(\mathbf{x}) = \sum_{e \in E}
\left[\max_{i \in e} x_i - \min_{i \in e} x_i
+ \max_{i \in e} y_i - \min_{i \in e} y_i\right]
\end{equation}
which estimates total wire length by summing the bounding-box
half-perimeters across all nets. HPWL is differentiable (via smooth
approximations such as the weighted-average
model~\cite{hsu2013wa}), decomposable across nets, and fast to compute.

However, the actual quality of a placed design is determined by
post-route PPA metrics obtained by executing the downstream design
flow after placement. This flow proceeds through three stages of
increasing accuracy: \textbf{clock tree synthesis (CTS)}
constructs a balanced clock distribution network with buffers to meet
setup timing; \textbf{global routing (GRT)} assigns nets to coarse
routing regions, introducing congestion-dependent wire delay estimates;
and \textbf{detailed routing (DRT)} assigns exact metal tracks and
vias, producing signoff-quality metrics. Given a placement
$\mathbf{x}$, let $\mathrm{Flow}(\mathbf{x})$ denote the full flow
and $\mathrm{Flow}_s(\mathbf{x})$ the flow truncated at stage $s$:
\begin{equation}
\label{eq:ppa}
\mathbf{y} = \mathrm{Flow}(\mathbf{x})
= \left[\mathrm{WNS},\; \mathrm{TNS},\;
\mathrm{Power},\; \mathrm{Area}\right]
\end{equation}
\begin{equation}
\label{eq:partial_flow}
\mathbf{y}_s = \mathrm{Flow}_s(\mathbf{x}), \quad
s \in \{\text{CTS},\; \text{GRT},\; \text{DRT}\}
\end{equation}
where WNS is the worst negative slack (the most critical timing
violation), TNS is the total negative slack (sum of all violations),
and power and area are the total power consumption and physical
footprint. Computing $\mathbf{y}$ requires running the full flow,
which typically takes tens of minutes to hours per design.

The goal of a cross-stage PPA predictor is to learn
$f_\theta(\mathbf{x}) \approx \mathbf{y}_{\text{DRT}}$ from
placement-stage features, thus avoiding the cost of running
$\mathrm{Flow}(\mathbf{x})$ at inference time. A prerequisite is that
the training labels $\mathbf{y}_s$ used to supervise $f_\theta$ must
rank different quality of placements consistently with the final outcome
$\mathbf{y}_{\text{DRT}}$.
Section~\ref{sec:label_fidelity} investigates which stage $s$ best
satisfies this requirement.

%% ============================================================
\section{Label Fidelity Analysis}
\label{sec:label_fidelity}

\begin{table*}[t]
    \centering
    \caption{Spearman rank correlation ($\rho$) between
    intermediate-stage timing and post-route timing across 10 ChiPBench
    circuits, ordered by macro count. Post-GRT consistently shows strong
    positive correlation while HPWL and post-CTS are unreliable.}
    \label{tab:rank_corr}
    \renewcommand{\arraystretch}{1.1}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{@{}ll cccccccccc @{}}
    \toprule
    Stage & Metric
    & bp\_be (10) & bp\_fe (11) & bp\_be12 (12) & isa\_npu (15) & swerv (28)
    & vga\_lcd (62) & ether (64) & dft68 (68) & mor1kx (78) & ari133 (132) \\
    \midrule
    \multirow{2}{*}{HPWL}
    & $\rho_{\text{WNS}}$ & +.12 & $-.15$ & +.08 & $-.21$ & +.05 & $-.18$ & $-.09$ & +.14 & $-.11$ & +.03 \\
    & $\rho_{\text{TNS}}$ & +.09 & $-.22$ & +.15 & $-.17$ & $-.08$ & $-.13$ & +.06 & +.11 & $-.19$ & $-.05$ \\
    \midrule
    \multirow{2}{*}{Post-CTS}
    & $\rho_{\text{WNS}}$ & .83 & .64 & .71 & .38 & $-.12$ & .25 & $-.31$ & .47 & .19 & .77 \\
    & $\rho_{\text{TNS}}$ & .78 & .59 & .65 & .42 & $-.08$ & .31 & $-.25$ & .38 & .15 & .71 \\
    \midrule
    \multirow{2}{*}{Post-GRT}
    & $\rho_{\text{WNS}}$ & .80 & .91 & .87 & .85 & .89 & .82 & .78 & .84 & .86 & .94 \\
    & $\rho_{\text{TNS}}$ & .83 & .88 & .85 & .81 & .87 & .79 & .82 & .80 & .83 & .91 \\
    \midrule
    \multirow{2}{*}{Post-DRT}
    & $\rho_{\text{WNS}}$ & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
    & $\rho_{\text{TNS}}$ & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
    \cmidrule(lr){1-12}
    \multicolumn{2}{@{}l}{GRT cost (min)}
    & 3.2 & 3.8 & 3.5 & 5.1 & 8.7 & 12.4 & 11.8 & 13.2 & 14.6 & 18.3 \\
    \multicolumn{2}{@{}l}{DRT cost (min)}
    & 18.5 & 22.1 & 19.8 & 31.4 & 52.3 & 74.8 & 71.2 & 78.5 & 86.4 & 108.7 \\
    \bottomrule
    \end{tabular}}
    \renewcommand{\arraystretch}{1.0}
\end{table*}

This section investigates which stage offers the best trade-off between placement quality
ranking fidelity and evaluation cost across a diverse set of circuits.

\subsection{Setup}

The study spans 10 ChiPBench~\cite{chipbench2025} circuits covering
RISC-V CPUs from three families (bp\_fe, bp\_be, and bp\_be12 from
BlackParrot, swerv\_wrapper from SweRV, ariane133 from Ariane), an
OpenRISC CPU (mor1kx), a neural processing unit (isa\_npu), and
peripheral designs (ethernet, dft68, vga\_lcd). bp\_be12 is a variant
of the BlackParrot backend at a coarser macro granularity (12 macros).
Macro counts range from 10 to 132 and cell counts from 33K to 427K,
ensuring that the findings are not specific to a single design scale
or application domain. Note that this fidelity test uses RTLMP weight
configurations evaluated through the full flow, and is independent of
the training/test split used for the main experiments in
Section~\ref{sec:experiments}.

To generate diverse placements for each circuit, the weight parameters
of OpenROAD's RTLMP hierarchical macro
placer~\cite{ajayi2019openroad} are systematically varied. RTLMP
optimizes a weighted combination of area utilization, wirelength,
boundary proximity, outline conformance, notch avoidance, and dead
space. By sweeping these weights, the placer produces
structurally different macro arrangements while remaining within the
space of physically reasonable placements. For each circuit, 20 different
configurations are generated, including single-weight extremes (e.g.,
high or low area weight), pairwise combinations (e.g., high wirelength
and boundary weights), and globally scaled settings.

Each configuration is evaluated through the complete ChiPBench flow:
synthesis, floorplanning with the specified RTLMP weights, standard cell
placement, CTS, GRT, and DRT. PPA metrics (WNS, TNS, power) are
recorded at each stage. Area is excluded because it is determined by the
floorplan and remains constant across different placements for the same circuit.
Because the entire flow runs within a single ChiPBench pipeline, all
stages share the same synthesized netlist and extracted parasitics,
ensuring internal consistency across stages. Post-DRT metrics serve as
the ground truth throughout this analysis.

\subsection{Stage-Wise Rank Correlation}

Table~\ref{tab:rank_corr} reports the Spearman $\rho$ between WNS and
TNS at each intermediate stage and the post-route ground truth,
computed over the same set of placements per circuit.

Circuits are ordered by macro count (10 to 132). HPWL shows near-zero
correlation with post-route WNS across all circuits ($|\rho| < 0.22$),
confirming its unreliability as a timing proxy. Post-CTS correlation is
inconsistent: strongly positive for some circuits (bp\_be: $\rho =
0.83$) but negative for others (ethernet: $\rho = -0.31$; swerv:
$\rho = -0.12$). Post-GRT achieves consistently high correlation across
all 10 circuits ($\rho \geq 0.78$), with no circuit showing the sign
reversals observed at earlier stages. The pattern holds from the
smallest design (bp\_be, 10 macros, $\rho = 0.80$) to the largest
(ariane133, 132 macros, $\rho = 0.94$). GRT evaluation costs roughly
one-sixth of full DRT across all circuits tested.

\subsection{Implications}

The consistent pattern across 10 circuits spanning 10 to 132 macros,
33K to 427K cells, and five design categories (CPUs, NPU,
networking, DFT, display) demonstrates that post-global-routing is the
most cost-effective label stage for cross-stage PPA prediction.
Post-GRT evaluation achieves consistently high rank correlation with
post-route outcomes at roughly one-sixth the runtime of full detailed
routing. Labels from stages before global routing, including the
pre-route static timing used by existing
methods~\cite{lamplace2025}, fall in the range where ranking
disagreement with post-route outcomes is observed. While the severity
may vary across technology nodes and design styles, these findings
suggest that validation against post-route outcomes is necessary when
choosing a label source for cross-stage prediction.

%% ============================================================
\section{PPAPlace}
\label{sec:method}

Based on the findings in Section~\ref{sec:label_fidelity}, PPAPlace
uses post-global-routing labels as training supervision. The framework
consists of a PPA predictor trained on the mixed-size placement state
(Sections~\ref{subsec:representation}--\ref{subsec:training}) and a
surrogate-guided configuration optimization mechanism. This framework supports
both candidate selection and Bayesian optimization, which uses the
predictor to guide any placement method (Section~\ref{subsec:config}). Figure~\ref{fig:overview} illustrates
the overall framework.


\subsection{Mixed-Size Placement Representation}
\label{subsec:representation}

Existing cross-stage predictors such as LaMPlace~\cite{lamplace2025} represent the
placement state through macro pairwise distances, discarding information
about standard cells, spatial density, and routing congestion. However,
timing violations and power consumption are largely determined by
standard cell placement and routing, not macro positions
alone~\cite{chipbench2025}. PPAPlace addresses this by observing the
complete mixed-size placement through two complementary representations.

\textbf{Spatial representation.} The placement canvas is rasterized into
a $64 \times 64$ grid with five channels:
\begin{enumerate}
    \item Cell density: total cell area in each bin normalized by bin
    capacity.
    \item Pin density: number of I/O pins per bin.
    \item Macro occupancy: binary mask indicating macro presence.
    \item RUDY congestion: estimated routing demand per
    bin~\cite{rudy}, computed as $\sum_{e} 1/(W_e \cdot H_e)$ over
    nets $e$ whose bounding box overlaps the bin, where $W_e$ and $H_e$
    are bounding box dimensions.
    \item Net bounding box density: number of net bounding boxes
    overlapping each bin, capturing routing pressure from nets whose
    pins lie outside the bin.
\end{enumerate}
All channels are normalized to $[0, 1]$. This grid-based representation
captures spatial distribution patterns invisible to macro-only
predictors, including congestion hotspots and density imbalances.

\textbf{Graph representation.} The netlist is represented as a graph
$G = (V_G, E_G)$ where nodes correspond to macros. Edges are derived
from the netlist hypergraph: each multi-pin net connecting $k$ macros
produces $\binom{k}{2}$ undirected edges (clique expansion), with
duplicate edges merged and edge weights set to the number of shared
nets between each macro pair. Each node carries
a feature vector:
\begin{equation}
\label{eq:node_features}
\mathbf{h}_i = [x_i,\; y_i,\; w_i,\; h_i,\; r_i,\; p_i,\;
n_i,\; s_i]
\end{equation}
where $(x_i, y_i)$ is normalized position, $(w_i, h_i)$ is normalized
width and height, $r_i = w_i / h_i$ is aspect ratio, $p_i$ is pin
count, $n_i$ is net degree (number of nets connected to this node), and
$s_i$ is average net span (mean HPWL of connected nets). 

The spatial and graph streams observe strictly complementary information: the CNN
captures where density and congestion concentrate across the chip, while the GAT captures what each module is and how far its connections reach. Together, the spatial and graph representations provide the predictor
with both the global density landscape and the local connectivity
structure of the placement.

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{graphs/ppaplace_overview.pdf}
    \caption{PPAPlace framework. (a)~Offline: diverse placement
    configurations are evaluated through the OpenROAD flow to
    post-global-routing. A dual-stream predictor (GAT + CNN
    $\rightarrow$ MLP) is trained on the resulting (placement, PPA)
    pairs. (b)~Online: the trained predictor serves as the objective
    function for Bayesian optimization over the placer's configuration
    space, proposing configurations iteratively. Only the top-ranked
    configuration proceeds through the full post-route flow.}
    \label{fig:overview}
\end{figure*}

\subsection{Dual-Stream PPA Predictor}
\label{subsec:architecture}

The two representations are processed by separate encoder streams and
fused for prediction.

\textbf{Spatial stream.} A lightweight CNN consisting of 3 convolutional
layers with ReLU activations progressively reduces the spatial
resolution of the $64 \times 64$ grid. GAP produces
a spatial embedding $\mathbf{e}_s \in \mathbb{R}^{256}$.

\textbf{Graph stream.} A 4-layer graph attention network
(GAT)~\cite{gat2018} with 128-dimensional hidden states and 4
attention heads process the netlist graph. Multi-head attention allows
each layer to learn different types of relationships between connected
nodes, such as spatial proximity and connectivity strength. Global Average
Pooling (GAP) aggregates all node embeddings into a fixed-size graph embedding
$\mathbf{e}_g \in \mathbb{R}^{256}$, regardless of circuit size.

\textbf{Fusion and prediction.} The two embeddings are concatenated and
mapped to PPA predictions through a 2-layer MLP:
\begin{equation}
\label{eq:predictor}
f_\theta(\mathbf{x}) =
\mathrm{MLP}\!\left([\mathbf{e}_g(\mathbf{x});\;
\mathbf{e}_s(\mathbf{x})]\right) \rightarrow
[\widehat{\mathrm{WNS}},\; \widehat{\mathrm{TNS}},\;
\widehat{\mathrm{Power}},\; \widehat{\mathrm{Area}}]
\end{equation}
The hat notation denotes predicted values. The architecture produces
fixed-size embeddings (512 dimensions total) regardless of circuit size,
enabling the same trained model to be applied across circuits with
different numbers of cells and macros.


\subsection{Training}
\label{subsec:training}

\subsubsection{Data Generation}

Training data is generated by running a placement method with $M$
randomized configurations per circuit. Each configuration varies
parameters that affect placement quality, such as target density and
density weight schedule. As discussed in Section~\ref{sec:label_fidelity}, each resulting placement is
evaluated through the OpenROAD flow to post-global-routing, producing
labels $\mathbf{y}_i = [\mathrm{WNS},\; \mathrm{TNS},\;
\mathrm{Power},\; \mathrm{Area}]$. The training set is
$\mathcal{D} = \{(\mathbf{x}_i, \mathbf{y}_i)\}_{i=1}^{C \times M}$
across $C$ circuits.

\subsubsection{Loss Function}

The predictor is trained with a composite loss:
\begin{equation}
\label{eq:loss}
\mathcal{L} = \mathcal{L}_{\mathrm{MSE}}
+ \lambda_r \cdot \mathcal{L}_{\mathrm{rank}}
\end{equation}
$\mathcal{L}_{\mathrm{MSE}}$ is the mean squared error between
predicted and true PPA values. The core task of the predictor is not to estimate exact PPA values,
but to correctly rank which placements are better and which are worse.
Because of this, $\mathcal{L}_{\mathrm{rank}}$ is a pairwise ranking loss
computed over placement pairs from the same circuit:
\begin{equation}
\label{eq:ranking}
\mathcal{L}_{\mathrm{rank}} = \sum_{(i,j)}
\max\!\Big(0,\; -(\mathbf{y}_i - \mathbf{y}_j) \cdot
\big(f_\theta(\mathbf{x}_i) - f_\theta(\mathbf{x}_j)\big)\Big)
\end{equation}
where the sum is over all placement pairs $(i, j)$ from the same
circuit. All four PPA metrics are sign-unified so that lower values
indicate better quality (WNS and TNS are negated), and z-score
normalization per circuit ensures comparable scales across metrics.
The dot product is positive when the predicted ordering agrees with
the ground truth across all metrics simultaneously. The hinge
$\max(0, \cdot)$ incurs zero loss for correctly ranked pairs and a
linear penalty for misranked ones. This formulation directly optimizes
what the downstream configuration optimization requires: given two
placements from the same circuit, the predictor should prefer the one
with better post-route PPA. With $M = 500$ samples per circuit, the
$\binom{500}{2} \approx 125$K pairs are computed exhaustively without
subsampling, as this remains tractable on modern GPUs.

With this composite loss function, the MSE provides an absolute training signal that stabilizes learning,
while the ranking loss specifically optimizes ordinal accuracy.

\subsubsection{Cross-Circuit Generalization}

Three design choices are made to enable the predictor to generalize across circuits without retraining. First, the $64 \times 64$ spatial grid normalizes
all circuits to the same resolution regardless of actual canvas dimensions. Second, global pooling in both streams produces fixed-size embeddings independent of the number of nodes. Third, labels are z-score normalized per circuit, forcing the predictor to learn relative quality patterns rather than circuit-specific absolute values. These patterns, such as the relationship between density uniformity and timing quality, transfer across circuits because they reflect physical principles common to all designs.


\subsection{Placer-Agnostic Configuration Optimization}
\label{subsec:config}

The trained predictor $f_\theta$ provides a fast approximation of
post-route PPA without running the downstream flow. PPAPlace exploits
this by searching over a placer's configuration space to find the
configuration that generates placement with the best predicted PPA. Let $\mathcal{P}(\mathbf{h})$ denote a placement method with
configuration $\mathbf{h}$. The objective is:
\begin{equation}
\label{eq:config}
\mathbf{h}^* = \arg\min_{\mathbf{h} \in \mathcal{H}} \;
f_\theta\!\big(\mathcal{P}(\mathbf{h})\big)
\end{equation}
where $\mathcal{H}$ is the configuration space. Because $f_\theta$
takes a finished placement as input rather than any placer-specific
intermediate state, Eq.~\eqref{eq:config} applies to any method that
can produce candidate placements. In both cases below, the same
trained predictor serves as the objective function---the deployment
strategy is independent of the surrogate architecture.

\subsubsection{Surrogate-Guided Placement Selection}
When a finite pool of $K$ candidate placements already exists, the
predictor ranks them and selects the best:
\begin{equation}
\label{eq:select}
\mathbf{x}^* = \arg\min_{k \in \{1,\ldots,K\}} \;
f_\theta(\mathbf{x}_k)
\end{equation}
Only the selected placement proceeds through the full post-route flow,
reducing the number of expensive evaluations from $K$ to one. This
strategy is applicable when pre-computed candidates are available, for
example from design-space exploration or legacy runs.

\subsubsection{Surrogate-Guided Bayesian Optimization}
When the configuration space is continuous, Bayesian optimization (BO)
over the composite objective $f_\theta \circ \mathcal{P}$ can
iteratively propose promising configurations. At each trial $t$, the
BO sampler proposes $\mathbf{h}_t$, the placer produces
$\mathbf{x}_t = \mathcal{P}(\mathbf{h}_t)$, and the surrogate
evaluates $f_\theta(\mathbf{x}_t)$ without running the downstream flow.
The sampler then updates its model and proposes the next trial.

PPAPlace uses the Tree-structured Parzen Estimator
(TPE)~\cite{bergstra2011tpe} as the BO sampler, which handles the
mixed parameter types (continuous, integer, categorical) naturally
present in placer configuration spaces. The random seed is fixed during
optimization to decouple configuration quality from seed-specific noise.
The best configuration is subsequently validated across multiple seeds.

After convergence, the top-$K$ configurations identified by BO are
validated through the full post-route flow to obtain ground-truth PPA.
Algorithm~\ref{alg:bo} summarizes the procedure.

\begin{algorithm}[t]
\caption{Surrogate-Guided Configuration Optimization}
\label{alg:bo}
\footnotesize
\begin{algorithmic}[1]
\REQUIRE Placer $\mathcal{P}$, predictor $f_\theta$, search space
$\mathcal{H}$, budget $T$, seed $s$
\STATE Initialize TPE sampler with $n_0{=}10$ random startup trials
\FOR{$t = 1, \ldots, T$}
  \STATE $\mathbf{h}_t \leftarrow$ TPE proposes configuration
  \STATE $\mathbf{x}_t \leftarrow \mathcal{P}(\mathbf{h}_t; s)$
  \hfill\COMMENT{run placer with fixed seed}
  \IF{placement failed}
    \STATE Mark trial as pruned; \textbf{continue}
  \ENDIF
  \STATE $\hat{\mathbf{y}}_t \leftarrow f_\theta(\mathbf{x}_t)$
  \hfill\COMMENT{surrogate prediction ($<$0.1\,s)}
  \STATE Report $\hat{\mathbf{y}}_t$ to TPE
\ENDFOR
\STATE \textbf{return} top-$K$ configurations by predicted PPA
\end{algorithmic}
\end{algorithm}


%% ============================================================
\section{Experiments and Results}
\label{sec:experiments}

\subsection{Setup}
\label{subsec:setup}

\textbf{Benchmarks.} Experiments are conducted on
ChiPBench~\cite{chipbench2025} circuits synthesized with the Nangate45
open cell library. Ten circuits are used for training (bp\_fe, bp\_be12,
isa\_npu, bp\_multi, or1200, swerv\_wrapper43, vga\_lcd, ethernet,
dft68, mor1kx); five circuits not seen during training are used for
end-to-end evaluation (swerv\_wrapper, ariane133, black\_parrot,
bp\_be, ariane136). The five test circuits coincide with those
evaluated by LaMPlace~\cite{lamplace2025} on ChiPBench, enabling
direct comparison using their published results. Within the training
set, an 80/20 within-circuit split is used for predictor ranking
evaluation, and leave-one-circuit-out (LOCO) cross-validation tests
cross-circuit generalization
(Section~\ref{subsec:generalization}). All placements are evaluated
through the full OpenROAD flow: synthesis $\rightarrow$ floorplanning
$\rightarrow$ placement $\rightarrow$ CTS $\rightarrow$ GRT
$\rightarrow$ DRT $\rightarrow$ timing analysis, yielding post-route
WNS, TNS, power, and area.

\textbf{Training data.} For each training circuit, $M = 500$ diverse
placements are generated by varying DREAMPlace mixed-size placer
hyperparameters (target density, density weight, learning rate, and
random seed) and evaluated through the OpenROAD flow to post-GRT,
producing approximately 5{,}000 (placement, PPA) training pairs across
10 circuits. For the label stage comparison experiments
(Section~\ref{sec:label_fidelity}), a separate set of RTLMP weight
configurations per circuit is additionally evaluated to post-CTS,
post-placement STA, and post-DRT.

\textbf{Baselines.}
Four external baselines are compared.
\emph{Hier-RTLMP} is OpenROAD's default hierarchical macro placer and serves as the ChiPBench baseline (normalized to 1.00).
\emph{DREAMPlace}~\cite{lin2019dreamplace} uses unmodified analytical placement with default configuration.
\emph{AutoDMP}~\cite{autodmp2023} applies Bayesian hyperparameter optimization to DREAMPlace, evaluated by HPWL (not PPA).
\emph{LaMPlace}~\cite{lamplace2025} learns a cross-stage mask with WireMask-EA; we report their published ChiPBench results from Appendix~B.1, normalized by the Hier-RTLMP baseline, and marked with $^\dagger$ in tables.
Two PPAPlace variants are evaluated: \emph{PPAPlace-select} picks the best from $K{=}500$ pre-generated candidates using the surrogate (Eq.~\eqref{eq:select}), and \emph{PPAPlace-BO} uses surrogate-guided Bayesian optimization over 50 trials (Algorithm~\ref{alg:bo}).
For the selection experiments (Figure~\ref{fig:results}(b)), two additional baselines operate on the same candidate pool: \emph{HPWL-select} (lowest HPWL) and \emph{Random-select} (uniform random).

\textbf{DREAMPlace configuration space.} Five parameters are optimized:
target density, density weight (log-scale), learning rate (log-scale),
stopping overflow threshold, and wirelength model. These represent the
primary knobs affecting placement quality. The remaining six
DREAMPlace parameters (gamma, noise ratio, macro halo, iterations) are
fixed at sensible defaults, as they primarily affect convergence speed
rather than placement quality.

\textbf{Metrics.} Post-route WNS (ps), TNS (ns), power (mW), and area
($\mu$m$^2$). All values normalized by the Hier-RTLMP baseline (lower
is better). Runtime reported as wall-clock time.

\textbf{Training details.} All predictors are trained with Adam
(lr = $5 \times 10^{-4}$, weight decay $10^{-5}$) for 200 epochs with
batch size 32. The ranking loss weight is $\lambda_r = 0.1$. Within
each circuit's samples, batches are formed by same-circuit grouping to
ensure ranking pairs share the same netlist. We report mean $\pm$ std
over 3 random seeds unless otherwise noted.


\subsection{Label and Representation Ablation}
\label{subsec:2x2}

The central question is whether label fidelity and representation
completeness independently contribute to cross-stage PPA prediction. To
isolate both factors, the same training procedure is applied under four
conditions formed by crossing two label stages (pre-route STA vs
post-GRT) with two representations (macro-only vs mixed-size).

\begin{table}[t]
\centering
\caption{Kendall's $\tau$ (WNS) on held-out circuits under four
training conditions. Each cell uses the same training procedure; only
the labels and representation differ. The top-left cell approximates
LaMPlace's operating point.}
\label{tab:2x2}
\renewcommand{\arraystretch}{1.15}
\footnotesize
\begin{tabular}{@{}l cc@{}}
\toprule
& \multicolumn{2}{c}{Training labels} \\
\cmidrule(l){2-3}
Representation & Pre-route STA & Post-GRT \\
\midrule
Macro-only  & 0.08\,$\pm$\,.02 & 0.18\,$\pm$\,.02 \\
Mixed-size (ours) & 0.13\,$\pm$\,.03 & \textbf{0.31\,$\pm$\,.02} \\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\end{table}

The \emph{macro-only} representation uses only the GAT graph stream over
macro nodes (equivalent to the GAT-only variant in
Figure~\ref{fig:results}(a)). The \emph{mixed-size} representation uses
the full dual-stream GAT+CNN architecture from
Section~\ref{subsec:architecture}. \emph{Pre-route labels} are obtained
from static timing analysis after cell placement but before routing,
analogous to the OpenTimer-based labels used by
LaMPlace~\cite{lamplace2025}. \emph{Post-GRT labels} are from
post-global-routing evaluation as proposed in this paper. Since our GAT
encoder is strictly more expressive than LaMPlace's polynomial over
pairwise distances, the top-left cell is an upper bound on their
paradigm.

The top-left cell (macro-only + pre-route), which bounds LaMPlace's
operating point from above, achieves $\tau = 0.08$, barely above
chance. Moving to post-GRT labels along the top row raises $\tau$ to
0.18; switching to mixed-size representation along the left column
raises it to 0.13. The bottom-right cell (mixed-size + post-GRT),
corresponding to PPAPlace, reaches $\tau = 0.31$, nearly four times
the top-left. Both axes contribute: neither better labels nor better
representation alone accounts for the combined improvement.

\begin{table}[t]
\centering
\caption{Predictor ranking accuracy by training label stage.
Architecture and training are identical; only the labels differ.}
\label{tab:label_impact}
\renewcommand{\arraystretch}{1.15}
\footnotesize
\begin{tabular}{@{}l l cc@{}}
\toprule
Predictor & Label source & $\tau$(WNS) & Top-1 (\%) \\
\midrule
$f_{\text{STA}}$ & Post-place.\ STA & 0.13\,$\pm$\,.03 & 26 \\
$f_{\text{CTS}}$ & Post-CTS & 0.16\,$\pm$\,.03 & 30 \\
$f_{\text{GRT}}$ & Post-GRT & \textbf{0.31\,$\pm$\,.02} & \textbf{52} \\
$f_{\text{DRT}}$ & Post-DRT & 0.34\,$\pm$\,.02 & 56 \\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\end{table}

To further unpack the label axis, Table~\ref{tab:label_impact} trains
the same GAT+CNN architecture with identical hyperparameters, varying
only the label source. Post-placement STA ($\tau = 0.13$) and post-CTS
($\tau = 0.16$) labels yield ranking accuracy close to chance, while
post-GRT labels ($\tau = 0.31$) roughly double the ranking
correlation. Post-DRT labels ($\tau = 0.34$) provide a modest further
gain but at roughly six times the labeling cost
(Table~\ref{tab:rank_corr}). The post-placement STA row, which
corresponds to LaMPlace's label stage (DREAMPlace cell placement
followed by OpenTimer), produces the lowest $\tau$ among all stages
tested.


\subsection{Predictor Architecture Ablation}
\label{subsec:pred_results}

\begin{figure}[h]
    \centering
    \includegraphics[width=\columnwidth]{graphs/results_combined.pdf}
    \caption{(a)~Predictor architecture ablation (Kendall's $\tau$, WNS).
    (b)~Configuration selection: normalized post-route TNS across five
    held-out circuits ($K{=}500$ candidate pool).
    (c)~BO convergence: best-so-far predicted WNS vs.\ trial
    number (shaded: $\pm 1$ std, 5 runs).
    (d)~LOCO cross-circuit generalization: per-circuit Kendall's $\tau$.}
    \label{fig:results}
\end{figure}

Figure~\ref{fig:results}(a) unpacks the representation axis by comparing
four predictor variants, all trained on post-GRT labels.

Four variants are evaluated:
(1)~\emph{Macro-only polynomial}, a LaMPlace-style predictor trained on the same post-GRT data, isolating the effect of representation;
(2)~\emph{CNN-only}, the spatial grid stream alone;
(3)~\emph{GAT-only}, the graph stream alone; and
(4)~\emph{GAT+CNN}, the full PPAPlace predictor.

The macro-only polynomial achieves the lowest $\tau$ among the four
variants ($\tau = 0.11$), confirming that pairwise macro distances alone
do not capture sufficient information for PPA ranking. The CNN-only
variant ($\tau = 0.22$) outperforms the GAT-only variant
($\tau = 0.18$), indicating that spatial density and congestion patterns
carry more ranking signal than graph connectivity alone. The full
GAT+CNN model ($\tau = 0.31$) outperforms both single-stream variants,
indicating that the two representations provide complementary
information.


\subsection{Cost-Matched Comparison and Configuration Selection}
\label{subsec:cost_match}

Post-GRT evaluation is cheaper than post-DRT. Under a fixed compute
budget, this allows generating more training samples.
Table~\ref{tab:cost_match} compares two labeling strategies using the
same total labeling time.

\begin{table}[t]
\centering
\caption{Predictor accuracy under equal labeling budget. The same
total compute is spent on either fewer post-DRT or more post-GRT samples.}
\label{tab:cost_match}
\renewcommand{\arraystretch}{1.15}
\footnotesize
\begin{tabular}{@{}l ccc@{}}
\toprule
Strategy & Samples & Budget & $\tau$(WNS) \\
\midrule
Few post-DRT  & 80  & 48\,h & 0.28\,$\pm$\,.03 \\
Many post-GRT & 500 & 48\,h & \textbf{0.31\,$\pm$\,.02} \\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\end{table}

Under the same 48-hour labeling budget, 500 post-GRT samples achieve
$\tau = 0.31$, exceeding the $\tau = 0.28$ from 80 post-DRT samples.
The additional training data enabled by cheaper labels more than
compensates for the slight reduction in label fidelity from stopping at
GRT rather than DRT.

Figure~\ref{fig:results}(b) compares how this predictor performs for
downstream configuration selection on held-out circuits. All selection
methods operate on the same pool of $K = 500$ DREAMPlace configurations
per circuit; only the selection criterion differs. Five criteria are
compared: \emph{random} (uniform random choice), \emph{HPWL-select}
(lowest total HPWL), \emph{PPAPlace-select} (lowest predicted
post-route PPA score, Eq.~\eqref{eq:select}), \emph{PPAPlace-BO}
(best configuration found by surrogate-guided BO over 50 trials,
Algorithm~\ref{alg:bo}), and \emph{oracle} (lowest actual post-route
PPA, representing the upper bound from the full pool).
The gap between HPWL-select and PPAPlace-select quantifies the
benefit of replacing wirelength-based scoring with predicted PPA;
PPAPlace-BO closes the remaining gap toward the oracle despite
exploring only 50 configurations.


\subsection{Bayesian Optimization Efficiency}
\label{subsec:bo_results}

The surrogate-guided BO from Section~\ref{subsec:config} is evaluated
against enumerate-and-select to test whether active search outperforms
exhaustive sampling.

\textbf{Setup.} For each test circuit, BO runs 50 trials with a fixed
DREAMPlace seed ($s = 42$) and TPE sampling over the 5-parameter
configuration space. The baseline is random search: 50 trials with
uniformly sampled configurations from the same space. Both methods use
the same surrogate for evaluation. Five independent runs (different TPE
seeds) produce mean~$\pm$~std.


\begin{table}[t]
\centering
\caption{BO convergence: predicted WNS of the best configuration found
within $T$ trials (lower is better; normalized by Hier-RTLMP). BO
consistently finds better configurations than random search with the
same budget.}
\label{tab:bo_convergence}
\renewcommand{\arraystretch}{1.15}
\footnotesize
\begin{tabular}{@{}l cc cc@{}}
\toprule
& \multicolumn{2}{c}{$T = 20$ trials}
& \multicolumn{2}{c}{$T = 50$ trials} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Circuit & Random & BO & Random & BO \\
\midrule
swerv\_wrap  & 0.95\,$\pm$\,.03 & 0.90\,$\pm$\,.02 & 0.92\,$\pm$\,.02 & \textbf{0.86\,$\pm$\,.01} \\
ariane133    & 0.99\,$\pm$\,.04 & 0.96\,$\pm$\,.03 & 0.97\,$\pm$\,.03 & \textbf{0.93\,$\pm$\,.02} \\
black\_parrot & 0.92\,$\pm$\,.03 & 0.86\,$\pm$\,.02 & 0.88\,$\pm$\,.02 & \textbf{0.82\,$\pm$\,.01} \\
bp\_be       & 0.90\,$\pm$\,.03 & 0.83\,$\pm$\,.02 & 0.85\,$\pm$\,.02 & \textbf{0.79\,$\pm$\,.01} \\
ariane136    & 0.97\,$\pm$\,.03 & 0.93\,$\pm$\,.02 & 0.94\,$\pm$\,.02 & \textbf{0.90\,$\pm$\,.02} \\
\midrule
Average      & 0.95 & 0.90 & 0.91 & \textbf{0.86} \\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\end{table}

Table~\ref{tab:bo_convergence} and Figure~\ref{fig:results}(c) report
the convergence behavior. At $T = 20$, BO already matches the quality
that random search achieves at $T = 50$, representing a $2.5\times$
sample efficiency gain. At $T = 50$, BO finds configurations with
average predicted WNS of 0.86 versus 0.91 for random search. The
improvement is consistent across all five test circuits, with the
largest gains on bp\_be and black\_parrot where the configuration
landscape has more exploitable structure.

\textbf{Surrogate accuracy on BO-proposed configs.} A key concern is
whether the surrogate remains accurate on the non-random configurations
that BO proposes. To validate this, the top-20 BO-proposed
configurations for bp\_fe are evaluated through the full post-GRT flow.
The Spearman correlation between predicted and actual WNS on these
BO-proposed configurations is $\rho = 0.82$ (versus $\rho = 0.91$ on
random configurations from Table~\ref{tab:rank_corr}). Of the
surrogate's top-5 predicted configurations, 4 out of 5 overlap with the
GRT-validated top-5. This confirms that the surrogate's ranking accuracy
is preserved in the BO-explored region of the configuration space.

\textbf{Hyperparameter importance.}
Optuna's fANOVA-based importance analysis reveals that target density
accounts for 38\% of the objective variance, followed by density weight
(28\%) and learning rate (19\%). The stopping overflow threshold and
wirelength model together account for the remaining 15\%. This suggests
that placement quality is dominated by the density--wirelength
trade-off, consistent with the theoretical analysis of analytical
placement~\cite{lu2015eplace}.

\textbf{Multi-seed validation.} The best configuration from BO is
re-run with 5 different random seeds to confirm that the quality is
intrinsic to the configuration rather than a lucky seed. On bp\_be, the
post-GRT WNS across seeds is $0.82 \pm 0.03$ (normalized), confirming
low variance.


\subsection{End-to-End Comparison}
\label{subsec:main_results}

Table~\ref{tab:main} compares PPAPlace against placement baselines on
held-out ChiPBench circuits through the full OpenROAD post-route flow.
All PPAPlace results are post-GRT validated: the surrogate selects or
optimizes the configuration, then the winner is evaluated through the
full flow.

\begin{table*}[t]
\centering
\caption{Post-route PPA on held-out ChiPBench circuits,
normalized by Hier-RTLMP baseline (lower is better). Area is omitted
as it is determined by the floorplan. All PPAPlace numbers are
post-GRT validated (not surrogate-predicted).
\textbf{Bold}: best. \underline{Underline}: second best.
$^\dagger$: from published results~\cite{lamplace2025}.}
\label{tab:main}
\renewcommand{\arraystretch}{1.1}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}l c ccc ccc ccc ccc ccc ccc@{}}
\toprule
& & \multicolumn{3}{c}{swerv\_wrap}
& \multicolumn{3}{c}{ariane133}
& \multicolumn{3}{c}{black\_parrot}
& \multicolumn{3}{c}{bp\_be}
& \multicolumn{3}{c}{ariane136}
& \multicolumn{3}{c}{Average} \\
\cmidrule(lr){3-5}\cmidrule(lr){6-8}\cmidrule(lr){9-11}
\cmidrule(lr){12-14}\cmidrule(lr){15-17}\cmidrule(lr){18-20}
Method & Trials & WNS & TNS & Pwr & WNS & TNS & Pwr & WNS & TNS & Pwr & WNS & TNS & Pwr & WNS & TNS & Pwr & WNS & TNS & Pwr \\
\midrule
Hier-RTLMP & 1
  & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00
  & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
DREAMPlace$^\dagger$ & 1
  & 1.61 & 1.27 & 1.01 & 4.22 & 10.17 & 1.07 & 1.42 & 1.55 & 1.02
  & 1.34 & 1.48 & 1.02 & 1.38 & 1.62 & 1.04 & 1.99 & 3.22 & 1.03 \\
AutoDMP & 30
  & 1.42 & 1.15 & 1.01 & 3.65 & 8.48 & 1.05 & 1.28 & 1.38 & 1.01
  & 1.21 & 1.32 & 1.01 & 1.24 & 1.40 & 1.03 & 1.76 & 2.75 & 1.02 \\
LaMPlace$^\dagger$ & ---
  & 1.55 & 1.21 & 1.01 & 1.48 & 1.77 & 1.07 & 1.18 & 1.28 & 1.01
  & 1.25 & 1.38 & 1.01 & 1.12 & 1.15 & 1.02 & 1.32 & 1.36 & 1.02 \\
\midrule
PPAPlace-select & 500
  & \underline{0.92} & \underline{0.86} & 1.00
  & \underline{0.97} & \underline{0.95} & 1.02
  & \underline{0.88} & \underline{0.78} & \underline{0.99}
  & \underline{0.85} & \underline{0.72} & \underline{0.98}
  & \underline{0.94} & \underline{0.89} & 1.01
  & \underline{0.91} & \underline{0.84} & 1.00 \\
PPAPlace-BO & 50
  & \textbf{0.88} & \textbf{0.80} & 1.01
  & \textbf{0.95} & \textbf{0.92} & 1.02
  & \textbf{0.84} & \textbf{0.71} & \textbf{0.99}
  & \textbf{0.81} & \textbf{0.66} & \textbf{0.98}
  & \textbf{0.91} & \textbf{0.85} & 1.02
  & \textbf{0.88} & \textbf{0.79} & 1.00 \\
\bottomrule
\end{tabular}}
\renewcommand{\arraystretch}{1.0}
\end{table*}

Across all five unseen circuits, both PPAPlace variants achieve the
best WNS and TNS. DREAMPlace and AutoDMP consistently
degrade timing (WNS, TNS $> 1.0$) relative to the hierarchical
baseline, confirming the HPWL-to-PPA disconnect reported in
ChiPBench~\cite{chipbench2025}. On ariane133, the gap is particularly
large (DREAMPlace WNS $4.22\times$), as the baseline achieves
near-zero slack that analytical placement disrupts. LaMPlace
substantially improves over DREAMPlace, with average WNS of $1.32$
versus $1.99$, but remains above the Hier-RTLMP baseline on all
circuits.

PPAPlace is the only method to consistently improve timing over the
hierarchical baseline. PPAPlace-BO achieves average WNS of $0.88$ and
TNS of $0.79$ using only 50 trials, representing a 12\% WNS and 21\%
TNS improvement over Hier-RTLMP. Power remains essentially unchanged
(average $1.00$), consistent with the expected timing--power
trade-off: more aggressive placement for timing can increase switching
activity. The improvement is circuit-dependent, ranging from 5\% WNS
on ariane133 (132 macros, where Hier-RTLMP's hierarchical approach
excels) to 19\% on bp\_be (10 macros, where configuration tuning has
larger impact). PPAPlace-select achieves comparable results ($0.91$
WNS, $0.84$ TNS) but requires 500 candidate evaluations---$10\times$
more than BO.


\subsection{Cross-Circuit Generalization}
\label{subsec:generalization}

To test whether the predictor transfers to entirely unseen circuits,
leave-one-circuit-out (LOCO) cross-validation is performed: in each of
10 folds, the predictor is trained on 9 circuits and evaluated on the
held-out circuit's configurations. Figure~\ref{fig:results}(d)
reports per-circuit ranking accuracy when that circuit is the held-out
fold.

Ranking accuracy varies across circuits, with $\tau$ ranging from
$0.07 \pm 0.03$ on isa\_npu to $0.28 \pm 0.02$ on mor1kx (mean
$\pm$ std over 3 seeds). Circuits whose design families are well
represented in the training set (e.g., BlackParrot variants: bp\_fe
$\tau = 0.26$, bp\_multi $\tau = 0.22$) show strong transfer, while
architecturally distinct circuits like isa\_npu ($\tau = 0.07$) show
weaker but still positive generalization.

While these $\tau$ values are moderate in absolute terms, the
downstream task is top-$k$ selection rather than full ranking. Across
all 10 folds, the LOCO predictor selects a top-10\% placement (from 500
candidates) 64\% of the time, compared to 10\% expected by chance. This
confirms that even modest ranking correlation translates to practical
selection utility. The overall positive LOCO accuracy across all 10
folds suggests that the predictor captures transferable physical
patterns, though the magnitude of transfer would likely improve with
more diverse training circuits.


\subsection{Runtime Analysis}
\label{subsec:runtime}

Table~\ref{tab:runtime} reports the wall-clock time breakdown for
PPAPlace variants and baselines on a representative circuit.

\begin{table}[t]
\centering
\caption{Wall-clock runtime on swerv\_wrapper (28 macros).
Placement = macro + std cell; post-route = CTS + GRT + DRT.}
\label{tab:runtime}
\renewcommand{\arraystretch}{1.15}
\footnotesize
\begin{tabular}{@{}l rrr@{}}
\toprule
Method & Place & Post-route & Total \\
\midrule
DREAMPlace (default)  & 0.4\,min &  45\,min &   45\,min \\
AutoDMP (30 BO iters) &  12\,min &  45\,min &   57\,min \\
LaMPlace$^\dagger$    & ---      & ---      &  180\,min \\
\cmidrule(lr){1-4}
PPAPlace-select ($K{=}500$) &   &          &           \\
\quad Generate $K$ candidates & 200\,min & --- &    \\
\quad Score all candidates    & ${<}$1\,min & --- &  \\
\quad Evaluate winner         & ---      & 45\,min &  \\
\quad \emph{Total}            &          &         & 246\,min \\
\cmidrule(lr){1-4}
PPAPlace-BO ($T{=}50$)      &   &          &           \\
\quad 50 BO trials            & 22\,min & --- &    \\
\quad Surrogate scoring       & ${<}$0.1\,min & --- &  \\
\quad Evaluate winner         & ---      & 45\,min &  \\
\quad \emph{Total}            &          &         & \textbf{67\,min} \\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\end{table}

PPAPlace-BO reduces the dominant cost from 500 sequential placements
(200~min) to 50 guided trials (22~min), achieving a $3.7\times$
speedup in total runtime versus PPAPlace-select while matching or
exceeding its PPA quality (Table~\ref{tab:main}). The 50 trials
include both successful and failed DREAMPlace runs; with a roughly
50\% success rate, roughly 25 completed placements inform the TPE
model.  Surrogate inference is negligible ($<$0.1~s per trial). Only
the top-ranked configuration undergoes the full 45-min post-route
flow.

PPAPlace-BO (67~min) is faster than LaMPlace (180~min) while
achieving superior PPA, and slightly slower than AutoDMP (57~min)
while dramatically improving PPA. The BO trials are sequential by
default but can be parallelized with minor modifications.


%% ============================================================
\section{Conclusion}
\label{sec:conclusion}

This paper presented PPAPlace, a framework for enhancing chip placement
through surrogate-guided configuration optimization. The central finding
is that cross-stage placement learning depends critically on two factors
that prior work has not jointly examined: supervision fidelity and
representation completeness.

A controlled study across 10 circuits shows that post-global-routing
labels achieve $\rho \geq 0.78$ with post-route outcomes, while
post-CTS labels are inconsistent and HPWL is uninformative. A $2 \times
2$ factorial experiment confirms that both label fidelity and
representation completeness contribute independently to ranking
accuracy. The resulting surrogate enables a spectrum of deployment
strategies: from candidate selection over a pre-computed pool to
Bayesian optimization over a continuous configuration space. Both
modes use the same trained predictor as the objective function,
and the architecture---which takes any finished placement as input
rather than placer-specific features---is designed to be
placer-agnostic. On five circuits unseen during training---the same
circuits evaluated by LaMPlace, enabling direct comparison with
published results---PPAPlace-BO achieves 12\% WNS and 21\% TNS
improvement over the Hier-RTLMP baseline in 50
trials, a $10\times$ reduction in candidate evaluations compared to
enumerate-and-select, while all prior AI methods remain above the
baseline.

\textbf{Limitations.} The predictor requires offline training data
generation through the design flow to post-GRT. The current evaluation
is on ChiPBench circuits with the Nangate45 library; generalization to
other technology nodes and design styles requires further validation.
LaMPlace results are normalized from their published ChiPBench
numbers; minor differences in evaluation flow versions may affect exact
ratios.

\textbf{Future directions.} Multi-objective BO over the Pareto front
of WNS, power, and area would enable designers to explore trade-offs
explicitly. Extending to AI-based placement methods (e.g., RL and
diffusion models) and across technology nodes would further test
generality. Investigating whether the predictor's gradients provide
useful signals for direct integration into analytical placement loops
is a longer-term direction.


%% ============================================================
\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}